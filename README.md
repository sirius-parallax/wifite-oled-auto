
# 📡 WiFi Audit Tool with OLED Display

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://python.org)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Linux%2FARM-orange.svg)]()
[![Wifite](https://img.shields.io/badge/Wifite-2.7.0-red.svg)](https://github.com/kimocoder/wifite2)

Автоматизированный инструмент для аудита безопасности WiFi сетей на базе одноплатного компьютера (NanoPi NEO, Raspberry Pi) с выводом информации на **OLED дисплей 128×128**.

Предназначен для **тестирования собственных сетей** на устойчивость к атакам. Работает автономно без подключения монитора или клавиатуры.

---

## 📖 Содержание

1. [Описание](#-описание)
2. [Возможности](#-возможности)
3. [Как работает скрипт](#-как-работает-скрипт)
4. [Оборудование](#-оборудование)
5. [Подключение OLED](#-подключение-oled-дисплея)
6. [Установка ПО](#-установка-по)
7. [Генерация словаря](#-генерация-словаря-паролей)
8. [Настройка скрипта](#-настройка-скрипта)
9. [Автозапуск (systemd)](#-автозапуск-systemd)
10. [Управление](#-управление-службой)
11. [Диагностика](#-диагностика-проблем)
12. [FAQ](#-faq)
13. [Предупреждение](#-предупреждение)

---

## 📋 Описание

Скрипт проводит **трёхэтапный тест** безопасности WiFi:

| Этап | Атака | Описание | Время |
|------|-------|----------|-------|
| **1** | WPS Pixie-Dust | Проверка на уязвимость WPS | ~5 мин |
| **2** | WPA Dictionary + PMKID | Атака по словарю с PMKID | ~10 мин |
| **3** | WPA Dictionary (Handshake) | Атака по словарю (хендшейк) | ~10 мин |

**OLED дисплей 128×128** переключается каждые **5 секунд**:
- 🟡 **ATTACK** — статус атаки, таймеры, цели
- 📜 **LOGS** — последние 9 строк логов wifite
- 🔴 **CRACKED** — найденные пароли (с миганием)
- 🟢 **RESULTS** — итоги после завершения цикла

После завершения всех атак скрипт **автоматически перезапускается** через 1 минуту и начинает новый цикл.

---

## ✨ Возможности

- ✅ **Автономная работа** — не требует монитора/клавиатуры
- ✅ **OLED 128×128** — большая площадь экрана, чёткий текст
- ✅ **Автозапуск** — запуск при включении устройства (systemd)
- ✅ **3 типа атак** — WPS, WPA+PMKID, WPA+Handshake
- ✅ **Логирование** — все события сохраняются в файл
- ✅ **Защита от сбоев** — автоматический перезапуск при ошибке
- ✅ **Современный UI** — индикаторы, анимация, разделители
- ✅ **Дата и время** — фиксация момента взлома каждой сети
- ✅ **Универсальность** — автоматическое определение путей
- ✅ **Циклическая работа** — бесконечные циклы атак 24/7

---

## ⚙️ Как работает скрипт

### 📊 Общая схема работы

```
┌─────────────────────────────────────────────────────────────┐
│                    ЗАПУСК УСТРОЙСТВА                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              systemd запускает wifi-audit.service           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│         Скрипт инициализирует OLED и проверяет WiFi        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    АТАКА 1: WPS Pixie-Dust                  │
│  • Сканирование сетей (60 сек)                              │
│  • Попытка взлома WPS (Pixie-Dust атака)                    │
│  • Если успех → сохранение PIN → показ на OLED              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                АТАКА 2: WPA Dictionary + PMKID              │
│  • Сканирование сетей (60 сек)                              │
│  • Захват PMKID у целевых сетей                             │
│  • Перебор паролей по словарю                               │
│  • Если успех → сохранение пароля → показ на OLED           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│            АТАКА 3: WPA Dictionary (Handshake Only)         │
│  • Сканирование сетей с клиентами (60 сек)                  │
│  • Деаутентификация клиентов                                │
│  • Захват 4-way Handshake                                   │
│  • Перебор паролей по словарю                               │
│  • Если успех → сохранение пароля → показ на OLED           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 ПОКАЗ РЕЗУЛЬТАТОВ (10 сек)                  │
│  • Все взломанные сети на OLED                              │
│  • Запись в лог-файл                                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    ПАУЗА (60 сек)                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 НОВЫЙ ЦИКЛ (шаг 1) ◀─────────────────────── │
└─────────────────────────────────────────────────────────────┘
```

---

### 📁 Структура файлов

```
/home/username/
├── wifi-audit-oled/
│   ├── wifi_audit.py          # Основной скрипт
│   └── hs/                     # Папка с хендшейками (от wifite)
│
├── wifite_full_log.txt        # Полный лог всех атак
├── cracked.json               # База взломанных сетей
└── .wifite/
    └── cracked.json           # Резервная копия взломов
```

### 📝 Формат лог-файла (`wifite_full_log.txt`)

```
=== WiFi Audit Started: 2025-03-06 15:00:00 ===
Attack started: 06.03 15:00
Home: /home/arkh
Interface: wlan0
Power: 40 dbm | Scan time: 60s
Dictionary: (wifite default)
Display: GME128128-01 (SH1106) 128x128

[WPS Pixie-Dust] [+] Scanning. Found 5 target(s)
[WPS Pixie-Dust] [+] (1/5) Starting attacks against TP-Link_CD14
[WPS Pixie-Dust] [+] WPS Pixie-Dust finished in 05m05s

[WPA Dict + PMKID] [+] Scanning. Found 8 target(s)
[WPA Dict + PMKID] [+] unknown (45db) PMKID CAPTURE: Waiting for PMKID
[WPA Dict + PMKID] [+] WPA Dict + PMKID finished in 10m15s

[WPA Dict (HS Only)] [+] Scanning. Found 3 target(s)
[WPA Dict (HS Only)] [+] Sending deauth to client
[WPA Dict (HS Only)] [+] Handshake captured
[WPA Dict (HS Only)] [+] WPA Dict (HS Only) finished in 08m30s

=== CRACKED RESULTS ===
Networks: 2
  TP-Link_CD14 : 07761560 (at 06.03 15:15)
  Keenetic-3437 : mypassword123 (at 06.03 15:22)
```

### 📝 Формат `cracked.json`

```json
{
  "targets": [
    {
      "essid": "TP-Link_CD14",
      "bssid": "AA:BB:CC:DD:EE:FF",
      "key": "07761560",
      "type": "WPA2"
    },
    {
      "essid": "Keenetic-3437",
      "bssid": "11:22:33:44:55:66",
      "key": "mypassword123",
      "type": "WPA2"
    }
  ]
}
```

---

### 🔄 Циклическая работа (systemd)

Служба настроена на **бесконечную циклическую работу**:

| Событие | Действие systemd | Время |
|---------|------------------|-------|
| Скрипт запущен | Мониторинг процесса | 0 сек |
| Скрипт выполняет атаки | Ожидание | ~30 мин |
| Скрипт завершён (exit 0) | Пауза `RestartSec=60` | 60 сек |
| Пауза завершена | Перезапуск скрипта | Новый цикл |
| Скрипт упал (error) | Пауза + Перезапуск | 60 сек |

**Проверка статуса:**
```bash
# Посмотреть сколько раз перезапускался
sudo systemctl show wifi-audit.service | grep -i restart

# Посмотреть время последнего запуска
sudo systemctl show wifi-audit.service -p ExecMainTimestamp
```

---

### 🖥️ Работа OLED дисплея

Дисплей автоматически переключается между режимами:

| Режим | Длительность | Что показывает |
|-------|--------------|----------------|
| **ATTACK** | 5 сек | Название атаки, таймеры, цель |
| **LOGS** | 5 сек | Последние 9 строк вывода wifite |
| **ATTACK** | 5 сек | Обновлённые таймеры |
| **LOGS** | 5 сек | Новые логи |
| **CRACKED** | 6 сек | При взломе — мигает 6 раз |
| **RESULTS** | 10 сек | После завершения атаки |

**Индикаторы в шапке:**
- `[*] ATTACKING` — идёт атака
- `[LOGS]` — режим логов
- `!!! CRACKED !!!` — найден пароль
- `[RESULTS]` — показ результатов
- `[ERROR]` — ошибка

**Индикатор в подвале:**
- `●` (заполненный круг) — атака активна
- `○` (пустой круг) — ожидание/пауза

---

### 🔐 Процесс взлома (по шагам)

#### Атака 1: WPS Pixie-Dust

```
1. Сканирование сетей с WPS
2. Выбор цели со слабым WPS
3. Отправка EAPOL запросов
4. Получение хеша от роутера
5. Офлайн подбор PIN (Pixie-Dust)
6. Получение WPS PIN → получение пароля
7. Сохранение в cracked.json
8. Показ на OLED (мигание 3 сек)
```

#### Атака 2: WPA Dictionary + PMKID

```
1. Сканирование сетей
2. Подключение к целевой сети
3. Захват PMKID из первого handshake
4. Отключение от сети
5. Перебор паролей по словарю
6. Если пароль найден → сохранение
7. Показ на OLED
```

#### Атака 3: WPA Dictionary (Handshake)

```
1. Сканирование сетей с клиентами
2. Выбор цели с активным клиентом
3. Отправка deauthentication пакетов
4. Клиент переподключается
5. Захват 4-way Handshake
6. Перебор паролей по словарю
7. Если пароль найден → сохранение
8. Показ на OLED
```

---

## 🛠️ Оборудование

| Компонент | Модель | Примечание | Цена |
|-----------|--------|------------|------|
| **Плата** | NanoPi NEO / Raspberry Pi | Любая с GPIO | $15-35 |
| **WiFi адаптер** | RTL8812AU / AR9271 | Обязательно режим монитора | $10-15 |
| **Дисплей** | OLED GME128128-01 (SH1106) | Интерфейс I2C, 128×128 | $5-8 |
| **Питание** | 5V 2A | Стабильное питание | $5 |
| **Провода** | Dupont Female-Female | 4 шт. для подключения | $2 |

**💰 Итого:** ~$40-65 за полное устройство

---

## 🔌 Подключение OLED дисплея

### Распиновка GME128128-01

| Пин OLED | Назначение | Цвет провода |
|----------|------------|--------------|
| **1** | VCC 3.3V | 🔴 Красный |
| **2** | GND | ⚫ Чёрный |
| **3** | SCL | 🟡 Жёлтый |
| **4** | SDA | 🟢 Зелёный |

### Схема подключения

```
OLED 128x128          NanoPi NEO / RPi
┌──────────          ┌──────────┐
│ VCC ●────┼───────────● 3.3V   │
│ GND ●────┼───────────● GND    │
│ SCL ●────┼───────────● SCL    │
│ SDA ●────┼──────────┼─● SDA    │
└──────────┘          └──────────
```

⚠️ **ВАЖНО:** 
- НЕ подключайте VCC к 5V! Дисплей рассчитан на 3.3V и может сгореть.
- Подключайте провода только при **выключенном питании**!

### Проверка подключения

```bash
sudo apt install i2c-tools -y
sudo i2cdetect -y 0
```

**Ожидаемый результат:** Должно показать **`3c`** на позиции 3c.

---

## 📦 Установка ПО

### Шаг 1: Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

### Шаг 2: Установка зависимостей

```bash
sudo apt install python3-pip git wifite aircrack-ng i2c-tools crunch -y
pip3 install luma.oled
```

### Шаг 3: Клонирование репозитория

```bash
cd ~
git clone https://github.com/ВАШ_НИК/wifi-audit-oled.git
cd wifi-audit-oled
```

---

## 🔑 Генерация словаря паролей

Для генерации словарей используется утилита **Crunch**.

### Быстрый старт

```bash
# Цифровой словарь 8 символов (рекомендуется)
crunch 8 8 0123456789 -o passwords.txt

# Цифры 4-6 символов (быстрый тест)
crunch 4 6 0123456789 -o passwords_small.txt

# Переместить в системную папку
sudo mv passwords.txt /usr/share/dict/
```

### Время генерации

| Длина | Символы | Паролей | Размер | Время |
|-------|---------|---------|--------|-------|
| 4-6 | Цифры | 1.1 млн | 8 MB | 1 мин |
| 8 | Цифры | 100 млн | 800 MB | 1 час |
| 8 | Цифры+буквы | 2.8 трлн | 22 TB | ❌ Не рекомендуется |

> **💡 Совет:** Можно оставить словарь пустым в настройках — wifite будет использовать встроенный словарь по умолчанию.

---

## ⚙️ Настройка скрипта

```bash
nano wifi_audit.py
```

**Измените переменные в начале файла:**

```python
INTERFACE = "wlan0"                              # Ваш WiFi интерфейс
DICTIONARY = ""                                  # Пусто = встроенный словарь wifite
WIFITE_POWER = 40                                # Мин. мощность сигнала (dbm)
WIFITE_SCAN_TIME = 60                            # Время сканирования (сек)
OLED_UPDATE_INTERVAL = 5                         # Обновление экрана (сек)
OLED_LOG_LINES = 9                               # Кол-во строк логов
```

> **✅ Пути к файлам определяются автоматически!**
> - Логи: `~/wifite_full_log.txt`
> - Взломы: `~/cracked.json`
> - Скрипт работает на любом ПК без изменения путей!

### Проверка работы

```bash
sudo python3 wifi_audit.py
```

Нажмите **Ctrl+C** для остановки после проверки.

---

## ⚙️ Автозапуск (systemd)

### Создание службы

```bash
sudo nano /etc/systemd/system/wifi-audit.service
```

**Вставьте содержимое (замените `username` на ваше имя пользователя!):**

```ini
[Unit]
Description=WiFi Audit Tool with OLED Display
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/username/wifi-audit-oled
ExecStart=/usr/bin/script -q -c "/usr/bin/python3 /home/username/wifi-audit-oled/wifi_audit.py" /dev/null

# === Циклическая работа ===
Restart=always           # Перезапускать ВСЕГДА
RestartSec=60            # Ждать 1 минуту перед рестартом

StandardOutput=journal
StandardError=journal
ExecStartPre=/bin/sleep 5

[Install]
WantedBy=multi-user.target
```

⚠️ **ВАЖНО: Замените пути на ваши!**

| Что заменить | Пример |
|--------------|--------|
| `/home/username/` | `/home/arkh/` |
| `wifi-audit-oled` | `wifi_oled` (если папка так называется) |

### Активация

```bash
sudo systemctl daemon-reload
sudo systemctl enable wifi-audit.service
sudo systemctl start wifi-audit.service
```

### Проверка

```bash
sudo systemctl status wifi-audit.service
sudo journalctl -u wifi-audit.service -f
```

> **ℹ️ Циклическая работа:**
> - Скрипт выполняет 3 атаки (~30 мин)
> - После завершения ждёт 1 минуту
> - Автоматически запускается заново
> - Работает бесконечно ♾️

---

## 📊 Управление службой

| Команда | Описание |
|---------|----------|
| `sudo systemctl status wifi-audit.service` | Показать статус |
| `sudo systemctl stop wifi-audit.service` | Остановить |
| `sudo systemctl start wifi-audit.service` | Запустить |
| `sudo systemctl restart wifi-audit.service` | Перезапустить |
| `sudo systemctl disable wifi-audit.service` | Отключить автозапуск |
| `sudo journalctl -u wifi-audit.service -f` | Логи в реальном времени |
| `sudo journalctl -u wifi-audit.service -n 50` | Последние 50 строк |

---

## 🔍 Диагностика проблем

### Дисплей не работает
```bash
sudo i2cdetect -y 0
# Должно показать 3c
```
**Решение:** Проверьте подключение проводов (VCC, GND, SCL, SDA). Убедитесь что VCC = 3.3V. Подключайте только при выключенном питании!

### Служба не запускается (ошибка CHDIR)
```bash
# Проверьте что папка существует
ls -la /home/username/wifi-audit-oled/

# Исправьте пути в службе
sudo nano /etc/systemd/system/wifi-audit.service

# Перезапустите
sudo systemctl daemon-reload
sudo systemctl restart wifi-audit.service
```

### Скрипт не запускается
```bash
sudo journalctl -u wifi-audit.service -n 100
sudo chmod +x wifi_audit.py
sudo systemctl restart wifi-audit.service
```

### WiFi адаптер не найден
```bash
iwconfig
sudo reboot
```

### Ошибка TTY / stty
```bash
# Проверьте что в службе есть script:
ExecStart=/usr/bin/script -q -c "..." /dev/null
```

### Взломанные сети не показываются
```bash
# Проверьте файл cracked.json
cat ~/cracked.json

# Если файл пустой — wifite ещё не взломал ни одну сеть
# Это нормально если все сети имеют сложные пароли
```

---

## ❓ FAQ

**Q: Можно использовать без OLED?**  
A: Да, скрипт работает, логи пишутся в `journalctl` и файл.

**Q: Какой словарь лучше?**  
A: Встроенный в wifite (по умолчанию) или `crunch 8 8 0123456789`.

**Q: Сколько времени на атаку?**  
A: ~25-30 минут на полный цикл (3 атаки) + 1 минута пауза.

**Q: Где взломанные пароли?**  
A: `~/cracked.json` и `~/wifite_full_log.txt`.

**Q: Как изменить интервал экрана?**  
A: В скрипте `OLED_UPDATE_INTERVAL = 5`.

**Q: Как остановить циклическую работу?**  
A: В службе измените `Restart=always` на `Restart=on-failure`.

**Q: Автовыключение после завершения?**  
A: Добавьте в конец функции `show_final_results()`:
```python
subprocess.run(["sudo", "shutdown", "-h", "now"])
```

**Q: Работает на Raspberry Pi?**  
A: Да, подключите OLED к GPIO (SCL/SDA/GND/3.3V).

**Q: Какой дисплей поддерживается?**  
A: GME128128-01 (SH1106) 128×128, SSD1306 128×64, другие совместимые с luma.oled.

---



---

**Теперь README содержит полное описание работы скрипта!** 🚀
